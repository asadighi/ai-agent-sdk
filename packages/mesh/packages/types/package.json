{
  "name": "@ai-agent/mesh/types",
  "version": "0.1.0",
  "private": true,
  "description": "types module for mesh",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3",
    "vitest": "^1.2.2",
    "rimraf": "^5.0.5"
  }
}