{
  "name": "@ai-agent/mesh",
  "version": "0.1.0",
  "private": true,
  "description": "mesh modules for AI Agent",
  "workspaces": [
    "packages/*",
    "packages/types",
    "packages/tests",
    "packages/common"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  }
}