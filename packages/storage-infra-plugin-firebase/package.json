{
  "name": "@ai-agent/storage-infra-plugin-firebase",
  "version": "0.1.0",
  "description": "Firebase Storage Infrastructure Plugin for AI Agent",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@ai-agent/multi-logger/types": "workspace:*",
    "firebase": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "eslint": "^8.0.0",
    "rimraf": "^5.0.0"
  },
  "peerDependencies": {
    "@ai-agent/multi-logger/types": "workspace:*"
  }
}